export const DEFAULT_SIZE = {
  width: 48,
  height: 28,
  activeSize: 24,
  margin: 2,
};

export const DEFAULT_GRADIENT_SIZE = {
  width: 57,
  height: 28,
  activeSize: 24,
  margin: 2,
};

// for android，默认overflow: hidden会导致阴影被裁，效果很差;
export const EXTRA_WIDTH = 2;
export const EXTRA_HEIGHT = 3;
